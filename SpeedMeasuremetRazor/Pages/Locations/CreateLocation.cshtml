@page
@model SpeedMeasuremetRazor.Pages.Locations.CreateLocationModel
@{
    ViewData["Title"] = "Create Location";
}

<h1>Create Location</h1>
<p>
    <a asp-page="Index">Back to List</a>
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="@Model.Location.Id" class="control-label"></label>
                <input asp-for="@Model.Location.Id" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="@Model.Location.Address" class="control-label"></label>
                <input asp-for="@Model.Location.Address" class="control-label" />
            </div>
            <div class="form-group">
                <label asp-for="@Model.Location.SpeedLimit" class="control-label"></label>
                <input asp-for="@Model.Location.SpeedLimit" class="control-label" />
            </div>
            <div class="form-group">
                @*<label asp-for="@Model.Location."*@
            </div>
        </form>
    </div>
</div>